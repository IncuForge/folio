import {NextResponse} from "next/server";import {insertRow,selectRows} from "@/lib/productivity-db";import {webSession} from "@/lib/web-session";export async function GET(){if(!await webSession())return NextResponse.json({error:"Unauthorized"},{status:401});return NextResponse.json(await selectRows("reminders"))}export async function POST(r:Request){if(!await webSession())return NextResponse.json({error:"Unauthorized"},{status:401});const b=await r.json();return NextResponse.json(await insertRow("reminders",{id:crypto.randomUUID(),entity_type:b.entityType||"general",entity_id:b.entityId||null,title:b.title,due_at:b.dueAt,recurrence:b.recurrence||null}),{status:201})}