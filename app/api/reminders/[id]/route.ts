import {NextResponse} from "next/server";import {updateRow} from "@/lib/productivity-db";import {webSession} from "@/lib/web-session";export async function PATCH(r:Request,{params}:{params:Promise<{id:string}>}){if(!await webSession())return NextResponse.json({error:"Unauthorized"},{status:401});const b=await r.json();return NextResponse.json(await updateRow("reminders",(await params).id,{status:b.status||"completed",completed_at:b.status==="pending"?null:new Date().toISOString()}))}