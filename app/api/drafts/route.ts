import {NextResponse} from "next/server";import {insertRow,selectRows,updateRow} from "@/lib/productivity-db";import {webSession} from "@/lib/web-session";
export async function GET(){const u=await webSession();if(!u)return NextResponse.json({error:"Unauthorized"},{status:401});return NextResponse.json(await selectRows("drafts",{user_id:u.id}))}
export async function PUT(request:Request){const u=await webSession();if(!u)return NextResponse.json({error:"Unauthorized"},{status:401});const b=await request.json(),id=b.id||crypto.randomUUID(),old=await selectRows("drafts",{id}),v={user_id:u.id,draft_type:b.draftType||"order",payload:b.payload,updated_at:new Date().toISOString()};return NextResponse.json(old.length?await updateRow("drafts",id,v):await insertRow("drafts",{id,...v}))}