import {NextResponse} from "next/server";import {updateRow} from "@/lib/productivity-db";import {webSession} from "@/lib/web-session";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){if(!await webSession())return NextResponse.json({error:"Unauthorized"},{status:401});const {id}=await params,b=await request.json();const values=Object.fromEntries(Object.entries(b).filter(([k])=>["name","phone","email","address","preferences","allergens","notes"].includes(k)));return NextResponse.json(await updateRow("contacts",id,{...values,updated_at:new Date().toISOString()}))}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){if(!await webSession())return NextResponse.json({error:"Unauthorized"},{status:401});await updateRow("contacts",(await params).id,{is_deleted:true,updated_at:new Date().toISOString()});return NextResponse.json({ok:true})}